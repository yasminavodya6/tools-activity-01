Tools Activity 01

This project demonstrates how to use POST and GET requests in Node.js with Express.

The user enters their name in a form. The name is sent using a POST request, then the server redirects to another page and displays a greeting using a GET request.
